{
  "name": "grameen-ai",
  "version": "2.0.0",
  "description": "Grameen AI — a family of AI agents (prices, farm inputs, government schemes, logistics) for Indian sellers and farmers, deployed on Netlify with a real Postgres backend (Netlify Database) and built on the SerpApi MCP server",
  "type": "module",
  "scripts": {
    "dev": "netlify dev",
    "deploy": "netlify deploy --prod"
  },
  "dependencies": {
    "@netlify/database": "^1.0.0",
    "bcryptjs": "^2.4.3",
    "jsonwebtoken": "^9.0.2"
  },
  "devDependencies": {
    "@types/bcryptjs": "^2.4.6",
    "@types/jsonwebtoken": "^9.0.7",
    "netlify-cli": "^19.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}
